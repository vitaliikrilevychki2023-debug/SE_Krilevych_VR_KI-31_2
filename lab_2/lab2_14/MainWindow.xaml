<Window
    Background="#ECECEC"
    Height="700"
    Title="MainWindow"
    Width="1000"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d"
    x:Class="lab2_14.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:lab2_14"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Window.Resources>
        <ResourceDictionary Source="../styles.xaml" />
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />  <!-- Для елементів пошуку -->
            <RowDefinition Height="*" />     <!-- Для основного контенту -->
        </Grid.RowDefinitions>

        <!-- Область для пошуку -->
        <StackPanel
            Orientation="Horizontal"
            HorizontalAlignment="Center"
            Margin="0,10,0,10"
            Grid.Row="0">
            <TextBox
                Height="40"
                Name="SearchTextBox"
                TextChanged="OnSearchTextChanged"
                Width="280"
                HorizontalContentAlignment="Center"
                VerticalContentAlignment="Center" />
        </StackPanel>

        <!-- Основний контент -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="5*" />
            </Grid.ColumnDefinitions>

            <!-- Кнопки зліва -->
            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <Button
                    Click="BookingsClick"
                    Content="Переглянути записи"
                    Height="50"
                    Margin="0,0,0,10"
                    Width="200" />
                <Button
                    Click="BonusClick"
                    Content="Переглянути кількість балів користувача"
                    Height="50"
                    Margin="0,0,0,10"
                    Width="200" />
                <Button
                    Click="Close"
                    Content="Вийти"
                    Height="50"
                    Margin="0,0,0,10"
                    Width="200" />
                <Button
                    Click="ViewingReviewsClick"
                    Content="Відгуки"
                    Height="50"
                    Margin="0,0,0,10"
                    Width="200" />
                <Button
                    Click="SignUpFormClick"
                    Content="Записатись на послугу"
                    Height="50"
                    Margin="0,0,0,10"
                    Width="200" />
                <Button
                    Click="DetailedInfoClick"
                    Content="Редагувати послугу"
                    Height="50"
                    Width="200" />
            </StackPanel>

            <!-- ScrollViewer справа -->
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Services}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button
                                Background="Gray"
                                Click="SelectService"
                                Content="{Binding Name}"
                                Height="50"
                                HorizontalAlignment="Stretch"
                                Margin="10"
                                Padding="10,5"
                                VerticalAlignment="Stretch"
                                Width="180" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
