<Window Background="#FFFFFF" Height="450" Title="Service Details" Width="400" WindowStartupLocation="CenterScreen" mc:Ignorable="d" x:Class="lab2_14.Pages.ServiceDetails" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Window.Resources>
        <ResourceDictionary Source="../styles.xaml" />
    </Window.Resources>
    <Grid>
        <Button Click="Close" Content="Вийти" Height="30" HorizontalAlignment="Right" Margin="10" VerticalAlignment="Top" Width="100" />
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Vertical">
                <TextBlock FontSize="20" FontWeight="Bold" Margin="0,0,150,10">Service Details</TextBlock>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Width="120">Name:</TextBlock>
                    <TextBox Text="{Binding Name}" Width="250" />
                </StackPanel>
                <StackPanel Margin="0,10,0,0" Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Width="120">Price:</TextBlock>
                    <TextBox Text="{Binding Price}" Width="250" />
                </StackPanel>
                <Button Click="UpdateButton_Click" Height="30" Margin="0,10,0,0" Name="UpdateButton" Width="150">Update Service</Button>
            </StackPanel>
            <StackPanel Grid.Row="2" HorizontalAlignment="Center" Margin="0,20,0,0" Orientation="Horizontal" />
        </Grid>
    </Grid>
</Window>