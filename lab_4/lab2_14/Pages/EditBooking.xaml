<Window
    Background="#ECECEC"
    Height="250"
    Title="EditBooking"
    Width="400"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d"
    x:Class="lab2_14.Pages.EditBooking"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:lab2_14.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Window.Resources>
        <ResourceDictionary Source="../styles.xaml" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Ім'я:" Margin="10,10,10,0" />
        <TextBox Grid.Row="0" Grid.Column="1" Margin="10,10,10,0" Text="{Binding Name}" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Номер телефону:" Margin="10,10,10,0" />
        <TextBox Grid.Row="1" Grid.Column="1" Margin="10,10,10,0" Text="{Binding Phone}" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Час запису:" Margin="10,10,10,0" />
        <TextBox Grid.Row="2" Grid.Column="1" Margin="10,10,10,0" Text="{Binding Time}" />

        <Button Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                Click="EditBookingClick"
                Content="Оновити запис"
                Height="40"
                HorizontalAlignment="Center"
                Margin="10,20,10,10"
                Width="191" />
    </Grid>
</Window>